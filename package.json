{
  "name": "stylework-lead-tracker",
  "version": "1.0.0",
  "description": "Stylework - Junior Full Stack Engineer Assignment: Lead Tracker Application",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix server run build && npm --prefix client run build",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "build:server": "npm --prefix server install && npm --prefix server run build",
    "start": "node server/dist/server.js",
    "test": "npm --prefix server test",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "keywords": [
    "lead-tracker",
    "react",
    "typescript",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Full Stack Engineer Candidate",
  "license": "MIT"
}
